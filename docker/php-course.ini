; Ajustes de PHP para el contenedor del curso.
; Sin un temp válido, las sesiones de CI3 y TCPDF fallan (mkdir(): Invalid path).
session.save_path = "/tmp"
sys_temp_dir = "/tmp"
upload_tmp_dir = "/tmp"
date.timezone = "Europe/Madrid"
memory_limit = 256M
